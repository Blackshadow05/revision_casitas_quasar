# Cloudinary Configuration
VITE_CLOUDINARY_CLOUD_NAME=dhd61lan4
VITE_CLOUDINARY_UPLOAD_PRESET=unsigned_preset

# Nota: Para subir imágenes puedes usar un Upload Preset (unsigned)
# ve a: Cloudinary Dashboard → Settings → Upload → Upload presets → Create upload preset
# Selección: Unsigned (sin firmar) para no exponer la API secret